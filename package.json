{
  "name": "ihhor-card",
  "version": "0.0.1",
  "type": "module",
  "description": "Custom Lovelace thermostat card (Ihhor Card) with HACS metadata and preview.",
  "scripts": {
    "dev": "vite dev --clearScreen false",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.0"
  }
}
